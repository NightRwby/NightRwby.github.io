$(document).ready(function () { $('#contact-form').on('submit', function (event) { event.preventDefault(); var btn = $(this).find('button'); var originalText = btn.text(); btn.text('SENDING...').prop('disabled', true); emailjs.sendForm('service_qktkpeu', 'template_lz2nkea', this).then(function () { alert('메세지가 성공적으로 전송되었습니다!'); btn.text(originalText).prop('disabled', false); $('#contact-form')[0].reset(); }, function (error) { alert('전송 실패... 잠시 후 다시 시도해주세요.\n에러: ' + JSON.stringify(error)); btn.text(originalText).prop('disabled', false); }); }); });